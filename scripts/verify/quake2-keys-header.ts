/**
 * File: quake2-keys-header.ts
 * Purpose: Keep the legacy `verify:keys:header` entrypoint aligned with the full `client/keys.c` verification.
 *
 * This file is not a direct source port.
 * It is a compatibility wrapper around the current key-system verification harness.
 */

import "./quake2-keys.ts";
