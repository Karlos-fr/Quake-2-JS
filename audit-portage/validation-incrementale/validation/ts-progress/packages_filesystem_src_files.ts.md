# Progress TS - packages/filesystem/src/files.ts

- Fichier TS: `packages/filesystem/src/files.ts`
- Matrice TS: `audit-portage/validation-incrementale/validation/ts-matrices/packages_filesystem_src_files.ts.md`
- Statut: Termine
- Dernier lot valide: fichier complet, 47 symboles audites en lot 20x.
- Prochain lot recommande: Aucun dans la matrice TS actuelle.
- Tests de reference:
  - `npm run verify:files`
  - `npm run verify:qcommon:header`
  - `npm run verify:web-config-gamedir`
  - `npm run verify:full-game:server-host`
  - `npm run verify:full-game:three-renderer`
  - `npm run typecheck`
- Decisions importantes:
  - Le deplacement de `qcommon/files.c` vers `packages/filesystem/src/files.ts` est accepte: les matrices C/H `qcommon_files.c.md` et `qcommon_qcommon.h.md` designent deja ce fichier comme proprietaire TS attendu pour les entites filesystem.
  - Les symboles porteurs de `qcommon/files.c` sont marques `Couvert C/H` uniquement quand `qcommon_files.c.md` les marque `Valide` avec `packages/filesystem/src/files.ts` comme cible.
  - `mountDirectory`, `mountPak` et `FS_Link` sont des adapters de compatibilite/manuels, pas les portages proprietaires de `FS_AddGameDirectory`, `FS_LoadPackFile` ou `FS_Link_f`.
  - Les interfaces, factories et helpers TypeScript sans source C/H ont `Original name: N/A`, `Source: N/A (...)` et `Category: New` explicites dans les entetes et la matrice.
  - `packages/formats/src/pak.ts` reste proprietaire du parsing PAK bas niveau; `files.ts` reste proprietaire du montage et de l'ordre des search paths.
- Blocages: Aucun.
