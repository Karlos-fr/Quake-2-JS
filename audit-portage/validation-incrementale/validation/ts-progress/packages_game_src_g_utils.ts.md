# Progress TS - packages/game/src/g_utils.ts

- Statut: Termine
- Dernier lot valide: validation croisee complete de `packages/game/src/g_utils.ts`.
- Symboles traites: helpers de pool `tv`/`vtos`, constantes locales `VEC_UP`/`MOVEDIR_UP`/`VEC_DOWN`/`MOVEDIR_DOWN`, fonctions portees de `G_ProjectSource` a `KillBox`, helpers locaux `equalsIgnoreCase`, `vectorLength`, `applyTelefragDamage`.
- Corrections appliquees:
  - Tentative d'import canonique `g_local.ts` pour `BODY_QUEUE_SIZE`, `TAG_LEVEL` et `MOD_TELEFRAG` rejetee apres test: elle cree le cycle ESM `g_local -> runtime -> g_utils -> g_local`.
  - Les miroirs locaux non exportes sont conserves dans `g_utils.ts` et documentes; le proprietaire exporte reste `g_local.ts`.
  - Import runtime inutilise `refreshEntitySpatialState` retire.
  - En-tetes des helpers `New` completes avec `Original name: N/A` et `Source: N/A (local helper)`.
- Tests de reference lances:
  - `npm run verify:g-utils`
  - `npm run verify:g-local:header`
  - `npm run typecheck`
- Integration runtime/apps/web/renderer-three:
  - Runtime: integre via les consommateurs `packages/game/src/*` et les exports publics `packages/game/src/index.ts`.
  - apps/web: non applicable directement; le flux navigateur consomme ces comportements via le runtime game/full-game, sans logique parallele detectee dans ce lot.
  - renderer-three: non applicable directement; ce fichier ne produit pas de donnees renderer propres, seulement des effets runtime ensuite exposes par les entites/etats.
- Decisions importantes:
  - `vectoyaw` a ete recontrole comme doublon potentiel; aucun autre symbole TS ne porte le meme couple `Original name` + `Source declaree`.
  - Les doublons locaux signales depuis `g_local.ts` (`BODY_QUEUE_SIZE`, `TAG_LEVEL`) ont ete traites explicitement; correction par import impossible sans cycle runtime.
  - `VEC_UP`, `MOVEDIR_UP`, `VEC_DOWN` et `MOVEDIR_DOWN` restent locaux dans `g_utils.ts`, car leur definition source est dans `game/g_utils.c`.
- Prochain lot recommande: aucun pour ce fichier.
- Blocages: aucun.
