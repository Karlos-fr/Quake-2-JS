# Progress TS - packages/renderer-three/src/refresh-entity-sync.ts

- Statut: Termine
- Dernier lot valide: gros lot final de 19 symboles, de `getRefreshEntityParent` a `disposeObject3D`, avec `getCachedMd2SkinTexture` ajoute car present dans le bloc TS mais absent de la matrice generee.
- Prochain lot recommande: aucun.
- Tests de reference:
  - `npm run verify:refresh-entity:alias-flags`
  - `npm run verify:refresh-entity:sprite`
  - `npm run verify:refresh-entity:weapon`
  - `npm run verify:gl-rmain`
  - `npm run verify:gl-mesh`
  - `npm run verify:gl-rsurf`
  - `npm run verify:full-game:three-renderer`
  - `npm run typecheck`
- Decisions:
  - `R_DrawEntitiesOnList`, `R_SetFrustum`, `R_CullAliasModel`, `R_DrawAliasModel`, `R_DrawSpriteModel` et `R_DrawBrushModel` gardent leurs proprietaires C/H dans `gl_rmain.ts`, `gl_mesh.ts` et `gl_rsurf.ts`.
  - Les symboles de ce fichier sont valides comme contrats/adapters Three.js; aucun n'est marque `Couvert C/H`.
  - Les entites `Category: New` du lot ont toutes `Original name: N/A` et une `Source declaree: N/A (...)`.
  - Le bloc final classe les helpers sprite/shadow/rotation derives de `ref_gl` comme `Adapter local`; les helpers de scene/camera/cache/disposal strictement Three.js restent `New` et `Hors C/H`.
- Integration:
  - Renderer-three verifie: entites visibles MD2, modeles, frames, skins, culling camera, interpolation, flags alias, sprites et view weapon via les scripts refresh-entity.
  - apps-web verifie: `createThreeRefreshEntitySync` est consomme par `apps/web` via le refresh host/full-game renderer et couvert par `verify:full-game:three-renderer`.
  - Runtime: consomme les `ClientRefreshFrame` produits par le client; pas de logique gameplay proprietaire dans cet adapter.
- Blocages: aucun.
