# Progress TS - packages/qcommon/src/messages.ts

- Statut: Termine
- Dernier lot valide: tout le fichier, 40 symboles (`READ_STRING_LIMIT` a `cloneUsercmd`).
- Verdict:
  - 27 fonctions `MSG_*` marquees `Couvert C/H` via `qcommon_common.c.md`.
  - 3 constantes locales marquees `Valide`, `Category: New`.
  - 8 constantes `CM_*` marquees `Valide`, `Category: Adapter`, car le proprietaire C/H attendu est `packages/qcommon/src/qcommon.ts`.
  - 2 helpers locaux marques `Valide`, `Category: New`.
- Preuves:
  - Matrice C/H `qcommon_common.c.md` verifiee: fonctions `MSG_*` en `Valide` avec proprietaire TS `packages/qcommon/src/messages.ts`.
  - Matrice C/H `qcommon_qcommon.h.md` verifiee: macros `CM_*` en `Valide` avec proprietaire TS `packages/qcommon/src/qcommon.ts`, donc pas de `Couvert C/H` dans `messages.ts`.
  - Source `Quake-2-master/qcommon/common.c` consultee pour les constantes de conversion, buffer string et fonctions `MSG_*`.
  - Recherche de doublons/ownership effectuee sur les couples `Original name` + `Source declaree`.
- Corrections:
  - En-tetes ajoutes dans `packages/qcommon/src/messages.ts` pour les constantes locales, adapters `CM_*`, `readStringInternal` et `cloneUsercmd`.
  - Matrice TS et avancement global mis a jour.
- Tests:
  - `npm run verify:qcommon:header`
  - `npm run typecheck`
  - `git diff --check`
- Integration:
  - Integre cote runtime qcommon/client/server via les primitives message, netchan, parsing serveur/client et delta usercmd/entity.
  - `apps/web` et `renderer-three` consomment les flux reseau/runtime produits; pas de correction dediee attendue pour ce lot.
- Prochain lot recommande: aucun pour ce fichier.
